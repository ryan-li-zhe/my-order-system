<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <title>My Order System Index</title>
    <style type="text/css">
      table{
        width: 800px;
        height: 400px;
        border: 1px solid #000;
        margin: 0 auto;
      }
      h1, div, p{
        text-align: center;
      }
      th {
        background-color: skyblue
      }
    </style>    
  </head>
  <body>
      <div>
      <table>
          <tr>
              <th>Cell Number</th>
              <th>Delivery Method</th>
              <th>Dishes</th>
              <th>Actions</th>
          </tr>
          <% for(var i=0; i < orders.length; i++){ %>
          <tr>
              <td><%= orders[i].cellNumber %></a></td>
              <td><%= orders[i].delivery %></td>
              <td><%= JSON.parse(orders[i].dishes) %> </td>
              <td>
                  <a href="/orders/<%= orders[i].cellNumber %>">Show Details</a> | 
                  <a href="#">Modifiy</a> | 
                  <a href="#">Delete</a>
                </td>
          </tr>
          <% } %>
      </table>
    </div>

    <div class="table-responsive" hidden="hidden">
      <table class="table">
        <thead>
          <tr>
            <th>Cell Number</th>
            <th>Delivery Method</th>
            <th>Dishes</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody id="tbody">

        </tbody>
      </table>
    </div>
    <div class="forwardTo">
    <p><a href="/">Go Home Page</a></p>
    </div>

    <script type="text/javascript" src="/jquery-3.5.1.min.js"></script>
    <script type="text/javascript">
    /***
      $.get("/orders", function(data){
        alert(JSON.stringify(orders));
        $.each(data, function(i, n){
          $tr = $("<tr></tr>");
          $("<td></td>").html(n.cellNumber).appendTo($tr);
          $("<td></td>").html(n.cellNumber).appendTo($tr);
          $("<td></td>").html(n.cellNumber).appendTo($tr);
          $tr.appendTo($("#tbody"));
        });
      });
    */
    </script>
  </body>
</html>